export const metadata = { title: "Editor | ColdReach AI", description: "ColdReach AI outreach editor." };

/** The authenticated shell owns the control-panel chrome; editor content is added here. */
export default function EditorPage() {
  return <div className="min-h-0 flex-1" />;
}
