{
  "name": "enterprise-Cold-Email-and-Outreach-Platform",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",

    "db:migrate": "bun run --cwd=packages/db prisma:migrate",
    "db:migrate:deploy": "bun run --cwd=packages/db prisma:migrate:deploy",
    "db:generate": "bun run --cwd=packages/db prisma:generate",
    "db:push": "bun run --cwd=packages/db prisma:push",

    "build:web": "turbo run build --filter=web",
    "start:web": "turbo run start --filter=web",

    "build:http": "turbo run build --filter=http-server",
    "start:http": "turbo run start --filter=http-server",
    "build:worker": "turbo run build --filter=worker",
    "start:worker": "turbo run start --filter=worker"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "dotenv-cli": "^11.0.0",
    "prettier": "^3.7.4",
    "turbo": "^2.9.16",
    "typescript": "5.9.2"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "bun@1.3.14",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "module": "index.ts",
  "type": "module",
  "dependencies": {
    "@t3-oss/env-nextjs": "^0.13.11",
    "dotenv": "^17.4.2",
    "prisma": "^7.8.0",
    "zod": "^4.4.3"
  }
}
