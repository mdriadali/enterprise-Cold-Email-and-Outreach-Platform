node_modules
.git
.turbo
.next
dist
out

# Environment files can contain credentials. Docker Compose reads .env.docker
# from the host at container startup; it must never enter a build context.
.env
.env.*
!.env.example
