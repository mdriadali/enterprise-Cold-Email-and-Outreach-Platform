name: Deploy Production

on:
  push:
    branches:
      - main

permissions:
  id-token: write
  contents: read

env:
  AWS_REGION: eu-north-1
  IMAGE_TAG: ${{ github.sha }}

jobs:
  # Job 1: Build & Push Images using Matrix on Self-Hosted Runner
  build-and-push:
    name: Build ${{ matrix.app.name }} Image
    runs-on: [self-hosted, Linux, X64]
    strategy:
      fail-fast: false
      max-parallel: 3
      matrix:
        app:
          - name: http-server
            dockerfile: ./apps/http-server/Dockerfile.http
            image_name: outreach-http
            build_args: ""
          - name: worker
            dockerfile: ./apps/worker/Dockerfile.worker
            image_name: outreach-worker
            build_args: ""
          - name: web
            dockerfile: ./apps/web/Dockerfile.web
            image_name: outreach-web
            build_args: |
              APP_NAME=Cold Out Reach
              HTTP_SERVER_URL=http://16.170.11.164:4000/api/v1/

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Configure AWS Credentials using OIDC
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build and Push Image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ${{ matrix.app.dockerfile }}
          push: true
          build-args: ${{ matrix.app.build_args }}
          tags: ${{ steps.login-ecr.outputs.registry }}/${{ matrix.app.image_name }}:${{ env.IMAGE_TAG }}
          cache-from: type=local,src=/tmp/.buildx-cache-${{ matrix.app.name }}
          cache-to: type=local,dest=/tmp/.buildx-cache-${{ matrix.app.name }}-new,mode=max

      # Move cache to prevent continuous growth on PC storage
      - name: Move Docker Cache
        run: |
          rm -rf /tmp/.buildx-cache-${{ matrix.app.name }}
          mv /tmp/.buildx-cache-${{ matrix.app.name }}-new /tmp/.buildx-cache-${{ matrix.app.name }} || true

  # Job 2: Trigger Deployment Script on EC2 via SSM
  deploy-to-ec2:
    name: Execute Remote Deployment via SSM
    needs: build-and-push
    runs-on: ubuntu-latest

    steps:
      - name: Configure AWS Credentials using OIDC
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Send Deployment Command to EC2
        run: |
          set -e

          COMMAND_ID=$(aws ssm send-command \
            --instance-ids "${{ secrets.EC2_INSTANCE_ID }}" \
            --document-name "AWS-RunShellScript" \
            --comment "Deploying version ${{ env.IMAGE_TAG }}" \
            --parameters 'commands=["bash /opt/outreach/deploy.sh '${{ env.IMAGE_TAG }}'"]' \
            --region "${{ env.AWS_REGION }}" \
            --query "Command.CommandId" \
            --output text)

          echo "SSM Command ID: $COMMAND_ID"
          echo "Waiting for EC2 deployment..."

          aws ssm wait command-executed \
            --command-id "$COMMAND_ID" \
            --instance-id "${{ secrets.EC2_INSTANCE_ID }}" \
            --region "${{ env.AWS_REGION }}"

          STATUS=$(aws ssm get-command-invocation \
            --command-id "$COMMAND_ID" \
            --instance-id "${{ secrets.EC2_INSTANCE_ID }}" \
            --region "${{ env.AWS_REGION }}" \
            --query "Status" \
            --output text)

          echo "Deployment status: $STATUS"

          echo "========== EC2 OUTPUT =========="
          aws ssm get-command-invocation \
            --command-id "$COMMAND_ID" \
            --instance-id "${{ secrets.EC2_INSTANCE_ID }}" \
            --region "${{ env.AWS_REGION }}" \
            --query "StandardOutputContent" \
            --output text

          echo "========== EC2 ERRORS =========="
          aws ssm get-command-invocation \
            --command-id "$COMMAND_ID" \
            --instance-id "${{ secrets.EC2_INSTANCE_ID }}" \
            --region "${{ env.AWS_REGION }}" \
            --query "StandardErrorContent" \
            --output text

          if [ "$STATUS" != "Success" ]; then
            echo "Deployment failed!"
            exit 1
          fi

          echo "Deployment successful!"